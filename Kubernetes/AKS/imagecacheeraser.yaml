apiVersion: eraser.sh/v1alpha1    # Enable addon first
kind: ImageList
metadata:
  name: imagelist
spec:
  images:
    - docker.io/library/alpine:3.7.3
# // You can also use "*" to specify all non-running images